# 任务清单

## 当前任务：仓库开源整理

- [x] 审查当前仓库中的开源风险点
- [x] 替换 `src/data/cards.js` 中不适合公开分发的外部素材与第三方 IP 引用
- [x] 新增 `README.md` 和 `LICENSE`
- [x] 补充 `package.json` 的开源元数据
- [x] 更新 `AGENTS.md` 同步新的文件结构和职责
- [x] 运行 `npm run build` 验证整理后仍可构建
- [x] 追加本次任务复盘

## 本次任务 Review

- 当前仓库不再依赖外部图片站点或第三方风格/IP 示例卡，`src/data/cards.js` 已替换为可公开分发的内联 SVG 示例数据。
- `README.md` 已补齐项目说明、运行方法、数据约定和开源边界，`LICENSE` 已补为 MIT。
- `package.json` 已增加项目描述、许可证和关键词元数据，便于公开仓库识别。
- 全量归档搜索框的示例关键词也已同步换成中性文案，避免公开仓库里继续残留旧示例指向。
- 已执行 `npm run build`，构建通过；新的示例数据包体积显著小于此前的大型外部卡库。
- 当前目录不是 Git 仓库，无法在本地完成 commit / push，需要你在初始化或接入 Git 后自行发布。
